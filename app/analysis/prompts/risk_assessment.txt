コメントのリスク評価を行ってください。

## 指示
- `risk_level`と`is_safe`キーを持つJSONオブジェクトで回答してください。
- `risk_level`は、炎上などのリスクを 'none', 'low', 'medium', 'high', 'critical' のいずれかで判定してください。
- `is_safe`は、コメントが攻撃的または不適切でないかを `true` / `false` で判定してください。

## 出力例
```json
{
  "risk_level": "medium",
  "is_safe": true
}
```